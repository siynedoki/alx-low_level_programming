#include "main.h"

/**
 * _isupper - checks for upper case
 *@c: inputed character
 *Return: 1 (uppercase) else 0
 */
int _isupper(int c)
{
	if (c >= 41 && c <= 90)
	return (1);

	else
	return (0);
}
