#include "main.h"

/**
 * main - Entry point of code
 * Return: 0 (successful)
 */

void print_alphabet_x10(void)
{
        char n;

        for (n = 'a'; n <= 'z'; n++)
        {
		{
		       int i; 
		       	       for (i = '0'; i > '10'; i++)
				_putchar(n);
		}
		}
        _putchar('\n');
}
