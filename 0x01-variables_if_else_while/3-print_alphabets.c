#include <stdio.h>

/**
 * main - Entry point of code
 * Return: 0 Always (Success)
*/

int main(void)
{
	char p;
	char o;

	p = 'a';
	o = 'A';
	while
	(p <= 'z')
	{
		putchar(p);
		p++;
	}
	while
	(o <= 'Z')
	{
		putchar(o);
		o++;
	}
	putchar('\n');
	return (0);
}
